import { describe, it, expect, beforeEach } from 'vitest'
import { Adaptor } from '@core/internal/index.js'

export function runTestsForAdaptor(
    adaptorInstance: Adaptor,
    adaptorName: string
) {
    describe(`Frsh with ${adaptorName}`, () => {})
}
